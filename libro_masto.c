#include "conf.h";

int main()
{
    transizione libro[SO_REGISTRY_SIZE][SO_BLOCK_SIZE];
    struct libro_mstro;
    //struct msgMIO msg;
    /*int blocco=0;
    int i=0,j=0;

    do 
    {
        while(msgrcv(MSG_KEY,&msg,sizeof(msgMIO)-sizeof(long),getpid(),0));/*in processo_nodo devo decidere a che ID mandare i mex
        libro[i][j]=msg.trn;
        i++;
        j++; 
    }while(1)(==SO_REGISTRY_SIZE);*/
}